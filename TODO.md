# TODO

- [ ] Implement SavingsCTA.handleShare to call POST /api/shares with current tools, receive {id}, copy /share/[id] URL.
- [ ] Ensure tool configs are correctly passed to /api/shares payload (use existing data available in SavingsCTA props).
- [ ] Keep other functions unchanged; only modify handleShare internals.
- [ ] Run TypeScript check / relevant tests (if feasible in terminal) to confirm no build errors.

